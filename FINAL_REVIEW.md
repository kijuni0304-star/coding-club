# 한빛코딩 최종 코드 감사

최종 검토일: 2026-09-15

## 결론

요구된 학교용 모의주식 핵심 흐름은 통합되어 있다. `user1`~`user10`의 세션별 데이터 격리, 회사 A~J의 공용 서버 가격, 분 단위 가격 갱신, 서버 현재가 기준 매수·매도, 포트폴리오 평가, 랭킹, 본인 거래내역과 모바일 다크 UI를 확인했다. 감사 중 발견한 명확한 오류와 동시성 위험은 기존 구조를 유지한 채 국소적으로 수정했다.

## 1. 발견한 문제

### 거래·시장 동시성

- 기존 프로세스 내부 잠금만으로는 여러 Flask 프로세스가 동시에 같은 예수금을 쓰거나 같은 주식을 두 번 팔 가능성이 있었다. SQLite에서 `SELECT FOR UPDATE`는 행 잠금을 제공하지 않아 이 위험을 막지 못했다.
- 주가 갱신 실행 키의 유일 제약만으로 결과 중복은 막을 수 있어도, 주문과 주가 갱신이 겹칠 때 오래된 ORM 값으로 검증할 여지가 있었다.
- 가격 갱신 도중 일부 종목만 처리된 뒤 오류가 나거나 정수 범위를 넘을 때 실행 기록과 가격의 원자성을 명확히 보장할 필요가 있었다.

### 인증·입력·오류 처리

- 로그아웃이 GET으로 가능했고 로그인 폼까지 일관된 CSRF 보호를 받지 못했다.
- 비정상 JSON, 배열·null, 중복 JSON 키, 중복 form 필드, 매우 큰 ID·수량, Unicode 위조 CSRF 토큰 등 경계 입력에서 400/404 대신 500이나 모호한 동작이 날 수 있었다.
- API의 넓은 예외 처리가 Flask의 404·413 같은 정상 HTTP 오류를 500으로 바꿀 수 있었다.
- 사용자 화면이 세션 닉네임 값에 의존할 여지가 있었고 세션 만료·응답 캐시 정책이 명확하지 않았다.

### 데이터 일관성·조회

- 예수금·보유량·가격을 여러 시점에 읽으면 거래가 진행되는 동안 포트폴리오 요약 내부 값이 서로 다른 스냅샷이 될 수 있었다.
- 거래내역 종목 접근은 행마다 추가 조회가 발생할 수 있었다.
- 랭킹을 표시용 소수 둘째 자리로 먼저 반올림해 정렬하면 실제 등락률이 다른 두 종목의 순서가 뒤바뀔 수 있었다.
- 최초 스키마 생성과 초기 데이터 삽입을 여러 프로세스가 동시에 시작하는 경우까지 쓰기 경계가 이어지지 않았다.

### 프런트엔드·문서

- 주문 화면 JavaScript에 가짜 가격 조정 코드와 임의 가격 상한이 남아 서버의 큰 실제 가격을 잘못 표시할 수 있었다.
- 동작하지 않는 뒤로가기/메뉴·가격 조절 UI와 중복 네비게이션/CSS가 있었고, 작은 화면에서 금액·수량 영역이 겹칠 여지가 있었다.
- 폴링 요청이 느릴 때 요청이 겹칠 수 있었고 주문 응답이 유실된 뒤 바로 재시도하면 중복 주문 위험을 사용자가 알기 어려웠다.
- README가 실제 구조, 안전한 DB 재생성, 필수 환경변수, 스케줄러 보장 범위와 테스트 방법을 충분히 설명하지 않았다.

## 2. 수정한 내용

- SQLite 쓰기 작업은 `database_write.py`의 즉시 쓰기 트랜잭션으로 직렬화했다. 매수·매도와 주가 갱신은 이 경계에 들어간 뒤 사용자·종목·잔액·보유량·서버 가격을 다시 읽고 하나의 커밋으로 처리한다.
- 분별 `PriceUpdateRun.minute_key` 유일 제약과 전체 종목 업데이트를 같은 트랜잭션에 묶었다. 중복 분만 정상적으로 건너뛰고 다른 무결성·범위 오류는 전체 롤백한다.
- 예수금 음수, 보유수량 음수, 평균 매수가 상태, 사용자/종목 보유행 중복, 거래 총액 일치 등을 DB 제약과 서버 검증 양쪽에서 방어했다.
- 모든 거래는 세션의 실제 `user_id`와 DB의 `Stock.current_price`를 사용한다. 클라이언트가 보낸 사용자·가격·총액은 신뢰하지 않는다. 전량 매도 시 평균 매수가는 0이 된다.
- 로그인·로그아웃 폼과 주문 POST에 CSRF 보호를 적용했다. 로그아웃은 POST 전용이며 로그인 성공 때 세션을 초기화한다. 세션은 8시간, HttpOnly, SameSite=Lax이며 비정적 응답은 `no-store`로 설정했다.
- JSON/form 파서를 엄격하게 만들고 크기 제한, 정수/양수/DB 범위, 중복 키·필드, 잘못된 종목을 일관된 4xx 응답으로 거절한다. DB 잠금 실패는 롤백 후 503을 반환한다.
- 포트폴리오는 로그인 사용자의 예수금·양수 보유행·현재가를 한 조회에서 구성하며, 계산식은 `portfolio_service.py`에 집중했다. 거래내역은 로그인 사용자 ID로 필터링하고 종목을 함께 조회한다.
- 랭킹은 반올림 전 정확한 비율로 정렬하고 화면·API는 동일한 계산 함수를 사용한다.
- 하드코딩 주가와 브라우저 가격 생성/조절을 제거했다. 화면은 15초마다 공용 API를 조회하며 요청 중복 방지와 10초 타임아웃을 적용했다. 주문 총액은 큰 정수도 안전하도록 `BigInt`로 계산한다.
- 공통 네비게이션과 공통 다크 테마를 분리하고 매수·상승은 빨강, 매도·하락은 파랑으로 통일했다. 사용하지 않는 UI 규칙을 정리하고 320px 폭까지 레이아웃을 보완했다.
- `SQLAlchemy`와 Windows 시간대 지원용 `tzdata`를 직접 의존성에 명시했다. README에 실행, 보안 설정, 구조, API, 공식, DB 백업 재생성, 테스트 및 운영 한계를 추가했다.

## 3. 실행한 검증

- 프로젝트의 기존 환경과 분리한 새 가상환경에서 `requirements.txt`만 설치했다.
- Python 3.14에서 기존 전체 단위·통합 테스트 **41개가 통과**했다(약 8.6초). 인증·사용자 격리·주문 검증·매수/매도·평균가·자산 계산·롤백·링크·정확한 랭킹과 다중 프로세스 경쟁을 포함한다.
- 이후 빈 SQLite DB를 두 프로세스가 동시에 최초 기동하는 테스트를 추가해 단독 통과시켰다. 테스트 모음은 현재 42개이며, 요청에 따라 이미 완료된 41개 전체 실행은 반복하지 않았다.
- 마지막 포트폴리오 단일 스냅샷 변경은 포트폴리오 전용 테스트와 `매수 → 가격 갱신 → 포트폴리오 → 매도 → 사용자 격리` 통합 사례를 각각 단독 재검증했다.
- `pip check` 결과 의존성 충돌이 없었다.
- 실제 예약기를 실행해 연속한 두 분의 고유 실행 기록과 가격 갱신을 확인했다.
- 실제 파일형 임시 SQLite DB에서 두 프로세스의 동시 매수, 동시 매도, 동시 가격 갱신, 주문과 가격 갱신 경쟁, 초기 데이터 중복 실행을 확인했다.
- 헤드리스 Chrome에서 로그인 → 실제 매수 → 실제 매도 → 거래내역 → 로그아웃을 확인했다. 랭킹의 15초 폴링 후 가격·순위·주문 링크 변경도 검사했다.
- 로그인·랭킹·포트폴리오·주문·거래내역을 320·375·480·1280px에서 검사해 JavaScript 오류, 가로 넘침과 주요 요소 겹침이 없음을 확인했다.
- 두 JavaScript 파일과 주요 Python 모듈의 문법 검사, Git 공백 오류 검사를 통과했다. 과거 가짜 금액·브라우저 랜덤 가격·죽은 인라인 이벤트 표식도 다시 검색해 남아 있지 않음을 확인했다.
- 기존 `instance/trading.db`는 변경하지 않고 읽기 전용으로 `integrity_check`와 외래키 위반을 검사했으며 이상이 없었다.

## 4. 남아 있는 한계

- 닉네임 로그인의 데이터 접근은 세션 사용자별로 분리되지만 비밀번호가 없어 실제 신원을 증명하지 못한다. 누구든 `user1`~`user10` 중 다른 이름으로 로그인할 수 있다. 이는 학교 수업용 간편 로그인 요구에 따른 의도된 한계다.
- 가격 갱신은 서버와 예약기가 실행 중일 때 같은 분에 **최대 한 번 성공**하도록 보장한다. 서버가 꺼져 있거나 DB가 해당 분 내내 잠겨 있으면 그 분은 누락되며 과거 분을 소급 갱신하지 않는다. `flask run` 또는 단순 WSGI import만으로는 예약기가 시작되지 않는다.
- SQLite 파일 DB를 로컬에서 쓰는 구성을 검증했다. 여러 서버 호스트가 네트워크 공유 SQLite 파일을 쓰는 운영 구조는 지원 범위가 아니다.
- 주문 요청 식별키가 없어 동일한 유효 POST를 재전송하면 별도 주문으로 체결된다. 브라우저의 연속 클릭은 막지만 네트워크 응답 유실 후에는 거래내역 확인이 필요하다.
- 평균 매수가는 정수 원 단위이므로 반복 부분 매수에서 소수 원가 반올림 오차가 누적될 수 있다. 전체 손익률은 현재 보유분의 미실현 손익률이며 실현손익을 포함한 계좌 누적수익률은 아니다.
- 거래내역과 분별 갱신 기록의 페이지네이션·보존 기간·정리 작업은 없다. 장기간 운영 전 추가하는 편이 좋다.
- 자동 DB 마이그레이션 도구는 아직 없다. 스키마 변경 시 백업 후 개발 DB를 재생성하거나 별도 마이그레이션을 작성해야 한다.

위 한계는 현재 학교용 단일 서버·10명 규모의 핵심 기능을 막지는 않지만, 외부 공개나 장기 운영으로 범위를 넓힐 때 우선 검토해야 한다.
