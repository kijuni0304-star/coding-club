# 한빛코딩 프로젝트 작업 지침

## 범위

이 프로젝트는 Flask와 Flask-SQLAlchemy로 만든 모의 주식 거래 웹 앱이다. 현재 로그인, 포트폴리오, 랭킹, 주문 화면의 기본 UI가 있으며, 화면 디자인은 기존 dark/mobile 앱 스타일을 최대한 유지한다.

## 변경 원칙

- 기능 요청과 직접 관련 없는 코드는 수정하지 않는다.
- 기존 템플릿 구조와 CSS 스타일을 우선 재사용하고, 필요한 데이터 표시만 Jinja 변수로 교체한다.
- 거래 로직은 라우트에 흩어 놓지 말고 별도 서비스/도메인 함수로 분리한다.
- 사용자를 세션의 닉네임 문자열만으로 식별하지 말고, 로그인 후 `user_id`를 세션에 저장하고 DB 외래키로 연결한다.
- 금액과 가격은 부동소수점 대신 정수 원 단위로 처리한다.
- 매수·매도는 잔액과 보유 수량 검증을 포함한 하나의 DB 트랜잭션으로 처리하고, 성공한 경우에만 거래 기록을 남긴다.
- 가격 변동 작업은 웹 요청마다 실행하지 않는다. 별도 스케줄러/작업 프로세스 또는 명시적인 테스트 가능한 갱신 함수로 분리한다.
- 초기 데이터 생성은 중복 실행에 안전해야 하며, 운영용 스키마 변경은 `create_all()`에 의존하지 않도록 마이그레이션 도입을 검토한다.
- 비밀번호 인증이 도입되기 전까지는 데모 로그인임을 명확히 하고, 실제 인증으로 확장할 때는 비밀번호 해시와 CSRF 방어를 사용한다.

## 권장 도메인 구조

- `User`: 사용자, 예수금
- `Stock`: 회사 A~J, 현재가, 기준가/직전 가격
- `Holding` 또는 확장된 포트폴리오 행: 사용자-종목별 보유 수량과 평균 매수가
- `Trade`: 매수/매도 방향, 수량, 체결가, 거래 금액, 체결 시각

종목명과 사용자 닉네임을 거래 데이터의 외래키로 직접 반복 저장하지 말고 숫자 ID를 사용한다. 사용자-종목 조합에는 유일 제약을 둔다.

## 검증 기준

변경 후 최소한 Flask 테스트 클라이언트로 로그인, 권한 없는 접근, 매수, 매도, 잔액 부족, 보유 수량 부족, 평균 매수가/평가손익 계산, 거래 기록 저장을 확인한다. UI를 수정한 경우 기존 세 화면의 모바일 레이아웃과 네비게이션도 확인한다.

## 현재 알려진 구조적 한계

랭킹과 주문 화면은 공용 `Stock` 데이터를 표시하며, 서버 스케줄러가 DB 실행 기록을 이용해 분당 한 번 가격을 갱신한다. 매수·매도 API는 서버 현재가로 예수금·보유량·평균 매수가·거래 기록을 한 트랜잭션에서 변경한다. 보유 종목별 평가금액·손익은 `portfolio_service.py`에서 계산해 포트폴리오 목록에 표시한다. `/history`는 로그인 사용자의 거래 기록만 최신순으로 표시하며, 표시 형식은 `transaction_history_service.py`에서 관리한다.
